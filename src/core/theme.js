import { DarkTheme, DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DarkTheme,
  colors: {
    ...DarkTheme.colors,
    primary: '#560CCE',
    secondary: '#414757',
    error: '#f13a59',
  },
}
